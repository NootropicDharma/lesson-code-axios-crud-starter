<div class="container">

    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-6">

                <h1>Edit {{data.name}}</h1>

                <hr>

                <form action="/movie-characters/edit/{{data.id}}" method="POST">
                    <label>
                        Name: <input type="text" name="name" value="{{data.name}}">
                    </label>

                    <label>
                        Occupation: <input type="text" name="occupation" value="{{data.occupation}}">
                    </label>

                    <label>
                        Weapon: <input type="text" name="weapon" value="{{data.weapon}}">
                    </label>

                    <input type="submit" value="Update character" class="btn-dark btn">
                </form>

            </div>
        </div>
    </div>

</div>